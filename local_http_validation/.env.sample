# =============================================================================
# Local HTTP Validation - Environment Configuration
# =============================================================================
#
# QUICK START: If you've already set up bearer-mcp-server/, just copy its .env:
#
#   cp ../bearer-mcp-server/.env .env
#   uv run test_http_mode.py
#
# The test script reads the same Azure Entra ID variables automatically.
#
# =============================================================================
# IMPORTANT: In HTTP mode, authentication is PER-REQUEST via HTTP headers.
# The MCP server does NOT use NEO4J_USERNAME/NEO4J_PASSWORD env vars.
# Instead, each request must include an Authorization header:
#   - Bearer token: Authorization: Bearer <jwt-token>
#   - Basic auth:   Authorization: Basic <base64(user:pass)>
# =============================================================================

# Neo4j Connection (server needs to know where Neo4j is)
NEO4J_URI=bolt://host.docker.internal:7687
NEO4J_DATABASE=neo4j

# MCP Server HTTP Settings
MCP_HTTP_HOST=0.0.0.0
MCP_HTTP_PORT=8080

# Test Endpoint (local Docker container)
MCP_ENDPOINT=http://localhost:8080

# =============================================================================
# Test Credentials (used by test script, NOT by the MCP server container)
# These are passed in HTTP request headers during testing
# =============================================================================

# -----------------------------------------------------------------------------
# Option 1: Azure Entra ID (automatic token acquisition)
# The test script will acquire a JWT using these credentials
# SAME VARIABLES AS bearer-mcp-server/.env - just copy it!
# -----------------------------------------------------------------------------
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
# AZURE_AUDIENCE=api://your-app-id  # Optional, defaults to api://{client_id}

# -----------------------------------------------------------------------------
# Option 2: Direct Bearer Token
# Use a pre-acquired JWT (from any IdP)
# -----------------------------------------------------------------------------
# MCP_BEARER_TOKEN=eyJhbGciOiJSUzI1NiIs...

# -----------------------------------------------------------------------------
# Option 3: Basic Auth (username/password)
# These are sent in the Authorization header as Base64
# -----------------------------------------------------------------------------
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=password
