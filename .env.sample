# =============================================================================
# Neo4j MCP Server - Azure Container Apps Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.sample .env
# =============================================================================

# =============================================================================
# Azure Configuration (Required)
# =============================================================================

# Your Azure subscription ID
# Find it: az account show --query id -o tsv
AZURE_SUBSCRIPTION_ID=

# Resource group name (will be created if it doesn't exist)
AZURE_RESOURCE_GROUP=neo4j-mcp-demo-rg

# Azure region for deployment
# Recommended: Choose a region close to your Neo4j database
AZURE_LOCATION=eastus

# =============================================================================
# Neo4j Database Connection (Required)
# =============================================================================

# Neo4j connection URI
# For Aura: neo4j+s://xxxxxxxx.databases.neo4j.io
# For self-hosted: neo4j://hostname:7687 or neo4j+s://hostname:7687
NEO4J_URI=

# Neo4j database name (usually "neo4j" for Aura)
NEO4J_DATABASE=neo4j

# Neo4j credentials
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=

# =============================================================================
# MCP Server Authentication (Required)
# =============================================================================

# API key for authenticating requests to the MCP server
# Generate a secure random string (minimum 32 characters)
# Example: openssl rand -base64 32
MCP_API_KEY=

# =============================================================================
# Optional Configuration
# =============================================================================

# Container app name (default: neo4j-mcp-server)
# CONTAINER_APP_NAME=neo4j-mcp-server

# Container registry name (default: auto-generated unique name)
# Must be globally unique, alphanumeric only, 5-50 characters
# ACR_NAME=

# Base name for resources (default: neo4jmcp)
# BASE_NAME=neo4jmcp

# Environment tag (default: dev)
# ENVIRONMENT=dev

# =============================================================================
# Neo4j MCP Server Path (Required for build)
# =============================================================================

# Path to the official Neo4j MCP server repository
# Clone from: https://github.com/neo4j/mcp
# The setup script will try to find this automatically in common locations
NEO4J_MCP_REPO=

# =============================================================================
# SSO Testing (Optional - for test-sso scripts)
# =============================================================================

# Azure AD test user for SSO testing
# Create with: ./scripts/create-user.sh
# Note: User must NOT have MFA enabled for ROPC flow testing
AZURE_TEST_USERNAME=
AZURE_TEST_PASSWORD=
