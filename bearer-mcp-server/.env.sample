# =============================================================================
# Bearer Token MCP Server - Environment Configuration
# =============================================================================
# Copy this file to .env and configure your settings.
#
# Bearer authentication mode:
# - No static API key required
# - No Neo4j username/password required
# - Authentication via JWT tokens from your identity provider
# - Requires Neo4j Enterprise with OIDC configured
# =============================================================================

# -----------------------------------------------------------------------------
# Azure Configuration (Required)
# -----------------------------------------------------------------------------

# Azure subscription ID
AZURE_SUBSCRIPTION_ID=

# Resource group name (will be created if it doesn't exist)
AZURE_RESOURCE_GROUP=neo4j-mcp-bearer-rg

# Azure region for deployment
AZURE_LOCATION=eastus

# -----------------------------------------------------------------------------
# Neo4j Configuration (Required)
# -----------------------------------------------------------------------------

# Neo4j connection URI
# Example: neo4j+s://xxxxxxxx.databases.neo4j.io
# Note: No credentials - authentication via bearer tokens
NEO4J_URI=

# Neo4j database name (default: neo4j)
NEO4J_DATABASE=neo4j

# -----------------------------------------------------------------------------
# Build Configuration (Required for deployment)
# -----------------------------------------------------------------------------

# Path to cloned Neo4j MCP server repository
# Clone from: https://github.com/neo4j/mcp
NEO4J_MCP_REPO=

# -----------------------------------------------------------------------------
# Optional Configuration
# -----------------------------------------------------------------------------

# Base name for Azure resources (default: neo4jmcp)
# Used as prefix for resource names
BASE_NAME=neo4jmcp

# Environment name: dev, staging, or prod (default: dev)
ENVIRONMENT=dev

# Read-only mode: true or false (default: true)
# When true, disables the write-cypher tool for safety
NEO4J_READ_ONLY=true

# CORS allowed origins (default: empty = disabled)
# Set to * to allow all origins, or comma-separated list of origins
# Example: https://myapp.com,https://localhost:3000
CORS_ALLOWED_ORIGINS=

# -----------------------------------------------------------------------------
# Identity Provider Configuration (for reference)
# -----------------------------------------------------------------------------
# These are not used by the deployment script, but are useful for reference
# when configuring your identity provider and client applications.

# Azure Entra ID (example)
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-app-id
# AZURE_CLIENT_SECRET=your-client-secret

# Okta (example)
# OKTA_DOMAIN=your-domain.okta.com
# OKTA_CLIENT_ID=your-client-id
# OKTA_CLIENT_SECRET=your-client-secret
